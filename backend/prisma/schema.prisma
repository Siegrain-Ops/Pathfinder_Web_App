generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Character {
  id        String   @id
  /// Full character sheet stored as a JSON string
  data      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
